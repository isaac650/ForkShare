// TODO(Claire): replace with real search/filter UI + recipe card grid.
// GET /api/recipes?search=&category=&maxTime= is already live on the backend.
function BrowsePage() {
  return (
    <div style={{ padding: 40, fontFamily: 'Helvetica, Arial, sans-serif' }}>
      <h2>Browse Recipes</h2>
      <p>Search/filter UI and recipe grid go here (Claire).</p>
    </div>
  );
}

export default BrowsePage;
